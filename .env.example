# Use localhost when running the Go API directly on your machine.
# Use db when running the Go API inside docker compose.
DB_HOST=localhost
DB_USER=mercari
DB_PASSWORD=secret
DB_NAME=mercari_db
DB_PORT=5432
